---
name: 产品分类树形选择优化
overview: 确认产品编辑表单的产品分类选择器已实现树形结构和语言过滤功能，并进行必要的优化
todos: []
isProject: false
---

# 产品分类树形选择优化计划

## 当前状态分析

### 已完成的功能

1. **HTML 模板** ([views/admin/product-edit-form.hbs](file://c:\code\pengcheng_website\views\admin\product-edit-form.hbs:29-34))
  - 分类选项包含 `data-lang-id` 和 `data-parent-id` 属性
  - 支持根据产品自动选中分类
2. **JavaScript 逻辑** ([views/admin/product-edit-form.hbs](file://c:\code\pengcheng_website\views\admin\product-edit-form.hbs:162-248))
  - ✅ 根据语言过滤分类
  - ✅ 树形结构展示（使用缩进和└─符号）
  - ✅ 语言变化时重新渲染分类
  - ✅ 保持选中状态
3. **后端数据** ([src/modules/admin/admin-product.controller.ts](file://c:\code\pengcheng_website\src\modules\admin\admin-product.controller.ts:103-119))
  - ✅ 传递所有分类（包含 langId 和 parentId）
  - ✅ 编辑时传递产品的 langId 作为默认语言

## 需要确认的功能

### 1. 树形结构显示

当前实现使用 JavaScript 动态构建：

```javascript
function addTreeOptions(items, level) {
  // 4 个空格缩进 + └─ 符号
  clone.textContent = indent + '└─ ' + opt.textContent;
}
```

### 2. 语言过滤

根据 `data-lang-id` 过滤：

```javascript
var optLangId = opt.getAttribute('data-lang-id');
return optLangId === selectedLangId;
```

## 优化建议

### 如果功能不完整，需要：

1. 确保分类数据包含正确的 `parentId` 和 `langId`
2. 优化树形缩进样式（使用 CSS 而非空格）
3. 添加视觉分隔线增强层级感

### 如果功能已完整，需要：

1. 测试验证功能正常工作
2. 可能只需要微调样式

## 下一步行动

由于代码已经实现了所需功能，建议：

1. **测试验证**：在浏览器中打开产品编辑页面
2. **检查效果**：
  - 选择不同语言，分类列表是否更新
  - 分类是否显示树形结构
  - 编辑产品时是否正确选中分类
3. **如有问题**：根据具体问题调整代码

